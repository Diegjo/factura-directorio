/** Señala cifras de lista, índices de portales o proyecciones sin confirmar. */
export function MarcaAproximado() {
  return (
    <span className="inline-flex items-center gap-1 rounded-full border border-brand/35 px-2.5 py-1 text-[10px] font-bold tracking-[0.1em] text-brand uppercase">
      Dato aproximado
    </span>
  );
}
