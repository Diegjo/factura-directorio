/** Señala cifras de lista, índices de portales o proyecciones sin confirmar. */
export function MarcaAproximado() {
  return (
    <span className="inline-flex items-center gap-1 rounded-full bg-brand-soft px-2 py-0.5 text-[10px] font-semibold tracking-[0.1em] text-brand uppercase">
      Dato aproximado
    </span>
  );
}
